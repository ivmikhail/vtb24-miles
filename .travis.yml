language: java
jdk:
  - oraclejdk8

sudo: required
before_install:
- "install-codacy-coverage-reporter.sh"

install: mvn install

after_success:
  - java -jar ~/codacy-coverage-reporter-assembly-latest.jar report -l Java -r target/site/jacoco/jacoco.xml